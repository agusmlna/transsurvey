<div class="table-wrap"><table><thead><tr><th>Klien / proyek</th><th>Kuesioner</th><th>Skor</th><th>Tanggal</th><th></th></tr></thead><tbody>@forelse($rows as $r)<tr><td><strong>{{ $r->client->name }}</strong><small>{{ $r->client->project }}</small></td><td>{{ $r->survey->title }}</td><td><span class="badge {{ $r->score!==null && $r->score<3.5?'amber':'green' }}">{{ $r->score===null?'—':number_format($r->score,2) }} / 5</span></td><td>{{ $r->submitted_at->format('d M Y H:i') }}</td><td><a class="icon-link" aria-label="Detail respons" href="{{ route('responses.show',$r) }}">↗</a></td></tr>@empty<tr><td colspan="5" class="empty">Belum ada respons untuk filter ini.</td></tr>@endforelse</tbody></table></div>
